{% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} col-md-6">
                            {{ message }}
                        </div>
                    {% endfor %}
{% endfor %}