<span class="badge bg-{{color}}" style="width:80px">
                                     {% if isArchived %}
                                          Archiver
                                      {% else %}
                                           Actif
                                     {% endif %}
</span>